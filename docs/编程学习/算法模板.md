## 开始

### 编译运行

run.sh

```
#/bin/bash

g++ -std=c++17 -O2 -Wall $1.cpp -o $1

./$1 < in > out

cat out
```

### 头文件

```c++
#include <bits/stdc++.h>
using namespace std;
using ll = long long;

// sgn 函数：判断符号，防精度问题，0返回0，正数返回1，负数返回-1
int sgn(ll x){return x==0?0:x>0?1:-1;}
```

### 二分

```c++
int search(vector<int> &nums, int target)
{
	int l = 0, r = nums.size() - 1;
	while (l < r)
	{
		int mid = (l + r + 1) >> 1;
		if (nums[mid] <= target)
		{
			l = mid;
		}
		else
		{
			r = mid - 1;
		}
	}
	if (nums[l] != target)
		return -1;
	return l;
}
```

python 版本：

```py
# 查找符合条件的最右值
def search(self, num:list[int], target: int) -> int:
	l, r = 0, len(num)-1
	while l < r:
		mid = (l + r + 1) // 2
		if num[mid] <= target:
			l = mid
		else:
			r = mid - 1
	if num[l] != target:
		return -1
	return l

# 查找符合条件的最左值
def search2(self, num:list[int], target: int) -> int:
	l, r = 0, len(num)-1
	while l < r:
		mid = (l + r) // 2
		if num[mid] >= target:
			r = mid
		else:
			l = mid + 1
	if num[r] != target:
		return -1
	return r
```

## 数学

### 快速幂

```c++
ll qpow(ll a, ll b, mod = MOD)
{
	ll res = 1;
	a%=mod;
	while(b)
	{
		if(b&1) res = res*a%mod;
		a=a*a%mod;
		b>>=1;
	}
	return res;
}
```

### 组合数

费马小定理：$a^{p−1}≡1(modp)⟹a⋅a^{p−2}≡1(modp)$。  
所以  a  的逆元就是  $a^{p−2}(mod p)$
f 是阶乘，g 是逆元

```c++
f[0]=1;
for(int i=1;i<=n;++i){
    f[i]=f[i-1]*i%MOD;
}
g[n]=qpow(f[n],MOD-2);
for(int i=n;i>=1;--i){
    g[i-1]=g[i]*i%MOD;
}
auto C=[&](int N,int K)->ll{
    if(K<0||K>N) return 0;
    return f[N]*g[K]%MOD*g[N-K]%MOD;
};
```

## 数据结构

### 并查集(DSU)

```c++
struct DSU
{
    vector<int> fa, sz;
    DSU(int n): fa(n+1), sz(n+1, 1)
    {
        iota(fa.begin(), fa.end(), 0);
    }
    int find(int x)
    {
        return fa[x] == x ? x : fa[x] = find(fa[x]);
    }
    bool same(int x, int y)
    {
        return find(x) == find(y);
    }
    int size(int x)
    {
        return sz[find(x)];
    }
    bool merge(int x, int y)
    {
        x = find(x), y = find(y);
        if(x == y) return false;

        fa[y] = x;
        sz[x]+=sz[y];
        return true;
    }
};
```

### 大根堆

```c++
priority_queue<int> pq;
pq.push(123);

```

### 单调栈

```c++
    vector<int> dailyTemperatures(vector<int>& temperatures) {
        stack<int> st;
        vector<int> ans(temperatures.size(), 0);
        for(int i = 0; i < temperatures.size(); i++)
        {
            if(st.empty() || temperatures[i]<=temperatures[st.top()])
            {
                st.push(i);
            }
            else
            {
                while(!st.empty() && temperatures[i]>temperatures[st.top()])
                {
                    ans[st.top()] = i - st.top();
                    st.pop();
                }
                st.push(i);
            }
        }
        return ans;
    }
```

## 图论

### 最短路径

堆优化`Dijkstra`求最短路，`dis[i]`：出发点 s 到各点 i 的最短距离

```c++
void solve()
{
    int n, m, s;
    cin >> n >> m >> s;
    vector<pair<int, int>> g[n + 1];
    for (int i = 0; i < m; i++)
    {
        int u, v, w;
        cin >> u >> v >> w;
        g[u].push_back({v, w});
    }
    vector<int> dis(n + 1, INT_MAX), vis(n + 1);
    priority_queue<pair<int, int>> q;
    q.push({0, s});
    dis[s] = 0;
    while (not q.empty())
    {
        int x = q.top().second;
        q.pop();
        if (vis[x])
            continue;
        vis[x] = 1;
        for (auto [v, w] : g[x])
        {
            if (dis[x] + w < dis[v])
            {
                dis[v] = dis[x] + w;
                q.push({-dis[v], v});
            }
        }
    }
    for (int i = 1; i <= n; i++)
    {
        cout << dis[i] << ' ';
    }
}
```

### 最小生成树

#### Kruskal

```c++
void solve()
{
    int n, m;
    int ans = 0;
    int cnt = 0;
    cin >> n >> m;
    DSU dsu(n);
    vector<array<int, 3>> g(m);
    for(int i = 0; i<m; i++)
    {
        cin >> g[i][1] >> g[i][2] >> g[i][0];
    }
    sort(g.begin(), g.end());
    for(auto [w, u, v]: g)
    {
        if(dsu.find(u) == dsu.find(v)) continue;
        ans += w;
        dsu.merge(u, v);
        cnt++;
        if(cnt == n-1) break;
    }
    if(cnt < n-1)
    {
        cout << "Graph discontinuity" << endl;
    }
    else
        cout << ans << endl;
}
```

## 动态规划

### 背包

0-1 背包 外围物品，内 target，外正序内倒序
完全背包 外围物品，内 target，外正序内正序
组合背包 外围 target，内物品，外正序内正序
多重背包

一维背包

```c++
void test_1_wei_bag_problem()
{
    vector<int> weight = {1, 3, 4};
    vector<int> value = {15, 20, 30};
    int bagWeight = 4;

    // 初始化
    vector<int> dp(bagWeight + 1, 0);
    for (int i = 0; i < weight.size(); i++)
    { // 遍历物品
        for (int j = bagWeight; j >= weight[i]; j--)
        { // 遍历背包容量(一定要逆序)
            dp[j] = max(dp[j], dp[j - weight[i]] + value[i]); //不取或者取第i个
        }
    }
    cout << dp[bagWeight] << endl;
}
```

[来源](https://leetcode.cn/problems/last-stone-weight-ii/solutions/805162/yi-pian-wen-zhang-chi-tou-bei-bao-wen-ti-5lfv)

## 计算几何

### 凸包

```c++
struct point{
	ll x,y,id;
	point(ll x,ll y,int id):x(x),y(y),id(id){}

	point operator-(point b){return point(x-b.x,y-b.y,0);}
	ll operator*(point b){return x*b.y-y*b.x;}
	// 重载小于号：用于求凸包前按坐标排序（先按 x 排序，x 相同按 y 排序）
	bool operator<(point b){return sgn(x-b.x)<0||(sgn(x-b.x)==0&&sgn(y-b.y)<0);}
	// 重载等于号：判断是否是同一个点（通过原始输入时的 id 判断）
	bool operator==(point b){return id==b.id;}
};
typedef vector<point> vp;

// 返回凸包顶点的个数，并将凸包顶点逆时针存入引用参数 w 中
int Andrew(vp &p,int n,vp &w){
	vp t=p;
	if(n<=2) {w=p;return n;} // 不到3个点，直接返回

	// 排序并去重（注：unique只去了相邻重复，但实际上这里不用unique也行）
	int tw=unique(t.begin(),t.begin()+n)-t.begin();
	sort(t.begin(),t.begin()+tw);
	w.clear();

	int v=0;
	for(int i=0;i<tw;i++){
		while(v>1&&sgn((w[v-1]-w[v-2])*(t[i]-w[v-2]))<=0) v--,w.pop_back();
		w.push_back(t[i]);
		v++;
	}
	int tp=v;
	for(int i=tw-1;i>=0;i--){
		while(v>tp&&sgn((w[v-1]-w[v-2])*(t[i]-w[v-2]))<=0) v--,w.pop_back();
		w.push_back(t[i]);
		v++;
	}
	if(tw>1) v--;
	return v;
}

// 求多边形面积的两倍（鞋带公式，叉乘累加）
ll area(vp &a){
	ll ins=0;
	for(int i=0;i<a.size();i++) ins+=a[i]*a[(i+1)%a.size()];
	return abs(ins);
}
```
